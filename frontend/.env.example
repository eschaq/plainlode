# Base URL of the Plainlode FastAPI backend.
# Defaults to http://127.0.0.1:8000 if unset (see src/lib/api.js).
# Point this at the deployed backend for production builds.
VITE_API_BASE_URL=http://127.0.0.1:8000
