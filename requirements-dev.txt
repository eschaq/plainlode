# Dev-only dependencies for the fine-tune and deployment scripts under finetune/
# (NOT needed by the deployed backend runtime). Install locally with:
#   pip install -r requirements-dev.txt
-r requirements.txt
fireworks-ai==0.19.20
kaggle
